#ifndef COREWORDS_H
#define COREWORDS_H

#include <stdlib.h>
#include "elem.h"

elem * plus(elem * stack);
elem * bye(elem * stack);
elem * showTop(elem * stack);
elem * showStack(elem * stack);

#endif
